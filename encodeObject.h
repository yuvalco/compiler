#ifndef PROJECT_ENCODEOBJECT_H
#define PROJECT_ENCODEOBJECT_H

#include "codeTable.h"
#include "dataTable.h"

void createObject(codeTable *cod, dataTable *dat, char *fileName, int ic, int dc);

#endif
